<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LepreCoinsApp.Views.MainPage"
             Title="LepreCoins - Семейный Бюджет"
             BackgroundColor="#0F1419">

    <Grid RowDefinitions="Auto,*" ColumnDefinitions="*" Padding="20">

        <!-- Заголовок -->
        <StackLayout Grid.Row="0" Spacing="10">
            <Label Text="Семейный Бюджет" FontSize="28" FontAttributes="Bold"/>
            <Label Text="Управляйте бюджетом семьи эффективно" FontSize="14"/>
        </StackLayout>

        <!-- Основное содержимое -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15" Padding="0,20,0,0">

                <!-- Карточка общего баланса -->
                <Frame BorderColor="#ddd" CornerRadius="10" Padding="20" HasShadow="True">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="Общий Баланс" FontSize="14"/>
                        <Label Text="{Binding TotalBalance, StringFormat='{0:C}'}" 
                               FontSize="32" FontAttributes="Bold" TextColor="#2ecc71"/>
                    </VerticalStackLayout>
                </Frame>

                <!-- Статистика -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                    <Frame Grid.Column="0" BorderColor="#ddd" CornerRadius="10" Padding="15" HasShadow="True">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Доходы" FontSize="12"/>
                            <Label Text="{Binding MonthlyIncome, StringFormat='{0:C}'}" 
                                   FontSize="18" FontAttributes="Bold" TextColor="#3498db"/>
                        </VerticalStackLayout>
                    </Frame>

                    <Frame Grid.Column="1" BorderColor="#ddd" CornerRadius="10" Padding="15" HasShadow="True">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Расходы" FontSize="12" TextColor="#666"/>
                            <Label Text="{Binding MonthlyExpenses, StringFormat='{0:C}'}" 
                                   FontSize="18" FontAttributes="Bold" TextColor="#e74c3c"/>
                        </VerticalStackLayout>
                    </Frame>
                </Grid>

                <!-- Действия -->
                <Label Text="Быстрые действия" FontSize="16" FontAttributes="Bold" Margin="0,10,0,0"/>

                <Button Text="+ Добавить расход" 
                        Command="{Binding AddExpenseCommand}"
                        BackgroundColor="#e74c3c" 
                        TextColor="White" 
                        CornerRadius="8"/>

                <Button Text="+ Добавить доход" 
                        Command="{Binding AddIncomeCommand}"
                        BackgroundColor="#2ecc71" 
                        TextColor="White" 
                        CornerRadius="8"/>

                <Button Text="📷 Сканировать чек"
        Command="{Binding ScanReceiptCommand}"
        BackgroundColor="#f39c12"
        TextColor="White"
        CornerRadius="8"/>


                <!-- Последние транзакции -->
                <Label Text="Последние транзакции" FontSize="16" FontAttributes="Bold" Margin="0,10,0,0"/>

                <CollectionView ItemsSource="{Binding Transactions}" SelectionMode="Single">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame CornerRadius="8" Padding="15" Margin="0,5">
                                <Grid ColumnDefinitions="*,Auto" ColumnSpacing="15">
                                    <VerticalStackLayout Grid.Column="0" Spacing="3">
                                        <Label Text="{Binding Description}" FontSize="14" FontAttributes="Bold"/>
                                        <Label Text="{Binding Date, StringFormat='{0:dd.MM.yyyy HH:mm}'}" 
                                               FontSize="12"/>
                                    </VerticalStackLayout>
                                    <Label Grid.Column="1" 
                                           Text="{Binding Amount, StringFormat='{0:C}'}" 
                                           FontSize="14" 
                                           FontAttributes="Bold"
                                           TextColor="{Binding Type, Converter={StaticResource TransactionTypeColorConverter}}"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>
        <!-- POPUP: Добавить/Редактировать операцию -->
        <Grid IsVisible="{Binding IsTransactionPopupVisible}"
      BackgroundColor="#80000000"
      Grid.RowSpan="2"
      ZIndex="100">

            <Frame BackgroundColor="{StaticResource SecondaryBg}"
           CornerRadius="18"
           Padding="18"
           WidthRequest="360"
           HorizontalOptions="Center"
           VerticalOptions="Center"
           HasShadow="True">

                <VerticalStackLayout Spacing="12">

                    <Label Text="{Binding TransactionPopupTitle}"
                   Style="{StaticResource HeaderLabel}"
                   FontSize="20"
                   Margin="0,0,0,6"/>

                    <Label Text="Сумма" Style="{StaticResource SubLabel}" />
                    <Entry Text="{Binding TransactionAmountText}"
                   Keyboard="Numeric"
                   Placeholder="Например: 1500.00"/>

                    <Label Text="Описание" Style="{StaticResource SubLabel}" />
                    <Entry Text="{Binding TransactionDescription}"
                   Placeholder="Например: Продукты / Зарплата"/>

                    <Label Text="Кошелёк" Style="{StaticResource SubLabel}" />
                    <Picker ItemsSource="{Binding Wallets}"
                    ItemDisplayBinding="{Binding Name}"
                    SelectedItem="{Binding SelectedWallet}" />

                    <Label Text="Категория" Style="{StaticResource SubLabel}" />
                    <Picker ItemsSource="{Binding Categories}"
                    ItemDisplayBinding="{Binding Title}"
                    SelectedItem="{Binding SelectedCategory}" />

                    <Label Text="{Binding TransactionFormError}"
                   TextColor="{StaticResource AccentOrange}"
                   IsVisible="{Binding HasTransactionFormError}"
                   FontSize="12"/>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,8,0,0">
                        <Button Text="Отмена"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding CancelTransactionCommand}" />

                        <Button Text="Сохранить"
                        Command="{Binding SaveTransactionCommand}" />
                    </Grid>

                </VerticalStackLayout>
            </Frame>
        </Grid>

    </Grid>

</ContentPage>


